/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: "#1E40AF", // Primary color (default shade)
          light: "#3B82F6", // Lighter variant
          dark: "#1E3A8A", // Darker variant
        },
        secondary: {
          DEFAULT: "#9333EA", // Secondary color (default shade)
          light: "#A855F7", // Lighter variant
          dark: "#7E22CE", // Darker variant
        },
      },
    },
  },
  plugins: [],
};
